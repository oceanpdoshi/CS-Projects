//#include "ScoreList.h"
//#include <iostream>
//#include <cassert>
//int main() {
//	//Don't need a destructor or copy constructor;
//	//Don't need to overload the assignment operator, it seems to be working fine.
//	ScoreList a;
//	ScoreList b;
//	a.add(1);
//	a.add(2);
//	a.add(3);
//	assert(a.remove(4) == false);
//	assert(a.add(115) == false);
//	
//	std::cerr << "minimum: " << a.minimum() << std::endl;
//	std::cerr << "maximum: " << a.maximum() << std::endl;
//	std::cerr << a.size() << std::endl;
//	assert(a.size() == 3);
//
//	b = a;
//	std::cerr << b.size() << std::endl;
//	assert(a.size() == b.size());
//	a.remove(1);
//	assert(a.size() == 2);
//	std::cerr << a.size() << std::endl;
//	b = a;
//	assert(b.size() == 2);
//	std::cerr << b.size() << std::endl;
//
//	ScoreList c = a;
//	assert(c.size() == 2);
//	std::cerr << c.size() << std::endl;
//
//	ScoreList d;
//	d.add(5);
//	d.add(1);
//	d.add(4);
//	d.add(0);
//	d.add(5);
//	d.add(5);
//
//
//}

int main() {}
